<%- include("../../partials/header") %>
<%- include("../../partials/sidebar") %>
           <div class="col py-3 mx-auto container-xxl ">

<!DOCTYPE html>
<html lang="en">
<head>
    <title>สายการแข่งขัน</title>

</head>
<body>
    <h1><%= tournamentName %></h1>

        <%- include("../../partials/menu_staff.ejs") %>

        <div class="card ">
            <% if(data.length) { %>
                <table class="table  w-75 mx-auto my-5 border  border-dark table-striped table-hover text-center">
                    <thead style="background: rgb(96, 166, 253); ">
                        <tr>
                            <th scope="col">ลำดับที่</th>
                            <th scope="col">ชื่อผู้เล่น</th>
                            <th scope="col">คะแนน</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < data.length; i++) { %>
                            <tr>
                                <th scope="row"><%= i+1 %></th>
                                <td><%= data[i].playerFName %></td>
                                <td><%= data[i].score %></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            <% } %>

            <!-- if result is empty-->
            <% if(!data.length) { %>
                <p class="text-center">ไม่พบรายการของการแข่งขัน</p>
            <% } %>
        </div>


          
</body>
</html>
</div>