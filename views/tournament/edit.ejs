
<%- include("../partials/sidebar") %>
        <div class="col-10 py-3 mx-auto">

<!DOCTYPE html>
<html lang="en">
<head>
    <title>tournament</title>
</head>
<body>

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>

    <% const CRstartdate = Rstartdate.toISOString().substring(0, 10);%>
    <% const CRenddate = Renddate.toISOString().substring(0, 10);%>
    <% const CtnmStartdate = tnmStartdate.toISOString().substring(0, 10);%>
    <% const CtnmEnddate = tnmEnddate.toISOString().substring(0, 10);%>
    
    <h1>หน้าแก้ไขการแข่งขัน <%= data[0].tnmName %></h1>
            <ul class="nav nav-pills w-100 justify-content-center">
                <td>
                    <a href="/tournament/edit/<%= data[0].tnmID %>" class="nav-link active">แก้ไขการแข่งขัน</a>
                    <a href="/tournament/bracket/<%= data[0].tnmID %>" class="nav-link active">จัดสายการแข่งขัน</a>
                    <a href="/tournament/participant/<%= data[0].tnmID %>" class="nav-link active">ผู้เข้าร่วม</a>
                    <a href="/tournament/match/<%= data[0].tnmID %>" class="nav-link active">แมทช์การแข่งขัน</a>
                    <a href="/tournament/highlight/<%= data[0].tnmID %>" class="nav-link active">ไฮไลต์</a>
                </td>
            </ul>

            
    <div class="card">
        <div class="card-header">
            แก้ไขกีฬา
        </div>
        <div class="card-body">
            <form action="/tournament/update/<%= tnmID %>" method="post" encType="multipart/form-data">
                <div class="form-group">
                    <label>ชื่อการแข่งขัน:</label>
                    <input type="text" class="form-control" name="tnmName" value="<%= tnmName %>">
                </div>
                <div class="form-group">
                    <label style="margin-top: 15px;">กีฬา:</label>
                    <select name="sportID">
                        <% for(let i = 0; i < data.length; i++) { %>
                        <option value="<%= data[i].sportID %>"><%= data[i].sportName %></option>
                        <% } %>
                    </select>
                </div>
                <div class="form-group">
                    <label>วันที่เปิดรับสมัคร:</label>
                    <input type="date" class="form-control" name="Rstartdate" value="<%= CRstartdate %>" autocomplete="off" required>
                </div>
                <div class="form-group">
                    <label>วันที่ปิดรับสมัคร:</label>
                    <input type="date" class="form-control" name="Renddate" value="<%= CRenddate %>" autocomplete="off" required>
                </div>
                <div class="form-group">
                    <label>วันที่เริ่มแข่ง:</label>
                    <input type="date" class="form-control" name="tnmStartdate" value="<%= CtnmStartdate %>" autocomplete="off" required>
                </div>
                <div class="form-group">
                    <label>วันที่สิ้นสุด:</label>
                    <input type="date" class="form-control" name="tnmEnddate" value="<%= CtnmEnddate %>" autocomplete="off" required>
                </div>
                <div class="form-group">
                    <label>เอกสารที่ใช้ในการสมัคร:</label>
                    <input type="text" class="form-control" name="tnmUrl" value="<%= tnmUrl %>" placeholder="บัตรประชาชน และ บัตรประจำตัวนักศึกษา">
                </div>
                <div class="form-group">
                    <label>รายละเอียด:</label>
                    <textarea type="text" class="form-control" name="tnmDetail" value="<%= tnmDetail %>"><%= tnmDetail %></textarea>
                </div>
                <div class="form-group">
                    <label>รูปภาพ:</label>
                    <input type="file" class="form-control" name="tnmPicture" value=" <%= tnmPicture %>">
                </div><br>
                <img src="/images/<%=tnmPicture %>" style="height: 300px; width: 300px;">
                <div class="form-group">
                    <label>ไฟล์:</label>
                    <input type="file" class="form-control" name="tnmFile1" value=" <%= tnmFile1 %>">
                </div>
                <br>
                <div class="form-group">
                    <a href="/tournament" class="btn btn-info">Back</a>
                    <input type="submit" class="btn btn-info" value="Update">
                </div>
            </form>

        </div>
    </div>

</body>
</html>
</div>