<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>uni</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body class="container" style="margin-top: 50px;">

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>

    
    <br><br>
    
    <div class="card">
        <div class="card-header">
            แก้ไขมหาวิทยาลัย
        </div>
        <div class="card-body">
            <form action="/uni/update/<%= uniID %>" method="post">
                <div class="form-group">
                    <label>ชื่อมหาวิทยาลัย:</label>
                    <input type="text" class="form-control" name="name" value="<%= name %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label style="margin-top: 10px;">สถานะ:</label>
                    <select name="status">
                        <option value="<%= status='ไม่เข้าร่วม' %>">ไม่เข้าร่วม</option>
                        <option value="<%= status='เข้าร่วม' %>">เข้าร่วม</option>
                    </select>
                </div>
                <br>
                <div class="form-group">
                    <a href="/uni" class="btn btn-info">Back</a>
                    <input type="submit" class="btn btn-info" value="Update">
                </div>
            </form>
        </div>
    </div>

</body>
</html>