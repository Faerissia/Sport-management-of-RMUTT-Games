<%- include("../partials/header") %>
<%- include("../partials/sidebar") %>
        <div class="col-10 py-3 mx-auto">

<!DOCTYPE html>
<html lang="en">
<head>
    <title>uni</title>
</head>
<body>

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>

    <br><br>

    <div class="card">
        <div class="card-header">
            เพิ่มมหาวิทยาลัย
        </div>
        <div class="card-body">
            <form action="/uni/add" method="post">
                <div class="form-group">
                    <label>ชื่อมหาวิทยาลัย:</label>
                    <input type="text" class="form-control" name="name" value="<%= name %>" autocomplete="off">
                </div>
                <div class="form-group">
                    <label style="margin-top: 10px;">สถานะ:</label>
                    <select name="status">
                        <option value="<%= status='เข้าร่วม' %>">เข้าร่วม</option>
                        <option value="<%= status='ไม่เข้าร่วม' %>">ไม่เข้าร่วม</option>
                    </select>
                    <br>
                </div>
                <div  style="margin-top: 10px;" class="form-group">
                    <a href="/uni" class="btn btn-info">ย้อนกลับ</a>
                    <input type="submit" class="btn btn-info" value="เพิ่ม">
                </div>
            </form>
        </div>
    </div>

</body>
</html>
        </div>